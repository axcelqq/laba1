# Репозиторий laba1
Мой первый проект
Этот проект создан для обучения работе с Git и Github

# Описание
Программа на С++ выводит параметры прямоугольника: периметр, площадь, диагональ.

# Содержимое репозитория
 
`rectangle.cpp` - программа для расчёта периметра, площади и диагонали прямоугольника |
`rectangle` - компилированный файл. |

# 1. Прямоугольник (rectangle.cpp)

Программа вычисляет:
- **Периметр** прямоугольника: `P = 2 × (a + b)`
- **Площадь** прямоугольника: `S = a × b`
- **Диагональ** прямоугольника: `d = √(a² + b²)`

# Автор - Кольчикова Алина
Дата - 22.03.26

# Программа
```cpp
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b;

    cout << "Введите стороны прямоугольника: ";
    cin >> a >> b;
    
    if (a <= 0 || b <= 0) {
        cout << "Ошибка! Стороны прямоугольника не могут быть отрицательными или равными нулю." << endl;
        return 1;
    }
    
    cout << "Периметр: " << 2 * (a + b) << endl;
    cout << "Площадь: " << a * b << endl;
    cout << "Диагональ: " << sqrt(a * a + b * b) << endl;
    
    return 0;
}


